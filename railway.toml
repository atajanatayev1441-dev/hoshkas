[build]
builder = "nixpacks"

[deploy]
startCommand = "cd server && npx prisma migrate deploy 2>/dev/null || npx prisma db push --accept-data-loss && node index.js"
healthcheckPath = "/api/categories"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
